# GPSM-1
GPSM (Generative Pre-trained State Machine) is a neural motion model trained on large-scale mocap data to predict and generate human motion, aiming to replace hand-crafted animation state machines used in traditional motion matching systems.
